# Multi-Site E2E Test Configuration
# Copy this file to .env and configure for your local environment

# ============================================================================
# SITE-SPECIFIC CONFIGURATION
# ============================================================================
# For each site defined in sites.config.ts, set the BASE_URL using the pattern:
# {ENV_PREFIX}_BASE_URL=https://your-site.docker.so.

# Core
ETUSIVU_BASE_URL="https://helfi-etusivu.docker.so"
SOTE_BASE_URL="https://helfi-sote.docker.so"
TYO_YRITTAMINEN_BASE_URL="https://helfi-elo.docker.so"
ASUMINEN_BASE_URL="https://helfi-asuminen.docker.so"
REKRY_BASE_URL="https://helfi-rekry.docker.so"
KASKO_BASE_URL="https://helfi-kasko.docker.so"
KUVA_BASE_URL="https://helfi-elo.docker.so"
KYMP_BASE_URL="https://helfi-kymp.docker.so"
HALLINTO_BASE_URL="https://helfi-strategia.docker.so"

# Other
PAATOKSET_BASE_URL="https://helsinki-paatokset.docker.so"

# ============================================================================
# GLOBAL CONFIGURATION
# ============================================================================

# Default base URL. If set, {ENV_PREFIX}_BASE_URL overrides this value.
# BASE_URL="https://www.stage.hel.ninja"

# Override storage state directory (optional)
# By default, uses XDG_RUNTIME_DIR, TMPDIR, or OS temp directory
# STORAGE_STATE_DIR=/custom/path

# Enable slow motion for headed tests (1 second delay between actions)
# Used by: npm run test:headed:slow
# SLOWMO=true

# CI mode
# CI=true
